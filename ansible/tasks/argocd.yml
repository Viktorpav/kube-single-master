- name: Install ArgoCD (node-failure safe, single Redis)
  kubernetes.core.helm:
    name: argocd
    chart_ref: argo/argo-cd
    release_namespace: argocd
    create_namespace: true
    wait: false
    values:
      controller:
        kind: Deployment
        replicas: 1
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 512Mi
        tolerations:
          - key: stateless-only
            operator: Equal
            value: "true"
            effect: NoSchedule
          - key: node.kubernetes.io/unreachable
            operator: Exists
            effect: NoExecute
            tolerationSeconds: 10

      server:
        replicas: 1
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 250m
            memory: 256Mi
        extraArgs:
          - --insecure
        tolerations:
          - key: stateless-only
            operator: Equal
            value: "true"
            effect: NoSchedule
          - key: node.kubernetes.io/unreachable
            operator: Exists
            effect: NoExecute
            tolerationSeconds: 10
          - key: node.kubernetes.io/not-ready
            operator: Exists
            effect: NoExecute
            tolerationSeconds: 10

      configs:
        params:
          server.insecure: "true"
      repoServer:
        replicas: 1
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 512Mi
        tolerations:
          - key: stateless-only
            operator: Equal
            value: "true"
            effect: NoSchedule
          - key: node.kubernetes.io/unreachable
            operator: Exists
            effect: NoExecute
            tolerationSeconds: 10
          - key: node.kubernetes.io/not-ready
            operator: Exists
            effect: NoExecute
            tolerationSeconds: 10

      applicationSet:
        enabled: false

      notifications:
        enabled: false

      dex:
        enabled: false

      redis:
        enabled: true
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
        persistence:
          enabled: false
        # Redis for ArgoCD is stateless in this setup (no persistence), can run on any node
        tolerations:
          - key: stateless-only
            operator: Equal
            value: "true"
            effect: NoSchedule
          - key: node.kubernetes.io/unreachable
            operator: Exists
            effect: NoExecute
            tolerationSeconds: 10
          - key: node.kubernetes.io/not-ready
            operator: Exists
            effect: NoExecute
            tolerationSeconds: 10

      redis-ha:
        enabled: false
  register: argocd_helm_install
  until: argocd_helm_install is success
  retries: 5
  delay: 15

# PodDisruptionBudgets removed to prevent blocking node drains in single-replica home lab setup

- name: Configure ArgoCD (Project, App, Ingress)
  kubernetes.core.k8s:
    state: present
    definition:
      - apiVersion: argoproj.io/v1alpha1
        kind: AppProject
        metadata:
          name: iooding
          namespace: argocd
        spec:
          sourceRepos: ["*"]
          destinations:
            - namespace: "*"
              server: "*"
          clusterResourceWhitelist:
            - group: "*"
              kind: "*"

      - apiVersion: argoproj.io/v1alpha1
        kind: Application
        metadata:
          name: iooding-blog
          namespace: argocd
          finalizers:
            - resources-finalizer.argocd.argoproj.io
        spec:
          project: iooding
          source:
            repoURL: https://github.com/Viktorpav/iooding.git
            targetRevision: master
            path: k8s/manifests
          destination:
            server: https://kubernetes.default.svc
            namespace: iooding
          syncPolicy:
            automated:
              prune: true
              selfHeal: true
            syncOptions:
              - CreateNamespace=true

      - apiVersion: networking.k8s.io/v1
        kind: Ingress
        metadata:
          name: argocd-ingress
          namespace: argocd
          annotations:
            cert-manager.io/cluster-issuer: "iooding-ca-issuer"
            nginx.ingress.kubernetes.io/ssl-redirect: "true"
            nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
        spec:
          ingressClassName: nginx
          tls:
            - hosts:
                - argocd.local
              secretName: argocd-server-tls
          rules:
            - host: argocd.local
              http:
                paths:
                  - path: /
                    pathType: Prefix
                    backend:
                      service:
                        name: argocd-server
                        port:
                          number: 80
  ignore_errors: true
